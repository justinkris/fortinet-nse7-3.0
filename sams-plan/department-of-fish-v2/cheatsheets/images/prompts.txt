IMAGE PROMPTS — Department of Fish v2 · Episode cheatsheets
==============================================================================

Every image is referenced by the sibling HTML pages via <img src="images/...">.
Feed the Prompt block into your image generator, save the PNG under the
filename below, and the placeholder in the page will swap in automatically.

==============================================================================
FROM: cheatsheet-episode-03-the-first-packet-chooses.html
------------------------------------------------------------------------------

Save as: images/ep03-front-desk-access-card.png

Page caption: The front desk is consulted once; the hallway runs on memory alone.

Author's suggested composition:
  Suggested illustration: a stylised "front desk" (labelled Inspection Plane /
  SD-WAN Rule Engine, showing health-check dials for port1 and port2) issuing
  a single access card to a packet, which then walks down a hallway (labelled
  Forwarding Plane / FIB) reusing the same card for every later packet — with
  a "closed window" showing the desk is never revisited.

Prompt (paste into image generator):
  Flat, minimal editorial-style illustration, warm cream and navy-blue
  palette. Left side: a reception "front desk" sign reading "INSPECTION PLANE
  — SD-WAN Rule Engine" with two small gauge dials labelled "port1" and
  "port2" showing health-check readings. A single paper packet icon approaches
  the desk and receives a punched access card. Right side: a long hallway
  labelled "FORWARDING PLANE — FIB / Session Table" where identical packet
  icons walk past, each holding the same access card, never stopping at any
  desk. Include a small "CLOSED — one visit only" sign hanging on the front
  desk facing the hallway side. No text other than the labels described.

------------------------------------------------------------

==============================================================================
FROM: cheatsheet-episode-04-the-implicit-rule.html
------------------------------------------------------------------------------

Save as: images/s1-implicit-rule-story.png

Page caption: Telstra Tower, mid-morning — Racks reopens a question nobody thought to ask.

Prompt (paste into image generator):
  A warm, editorial-style illustration of a modern open-plan office floor
  inside a tower building (Telstra Tower, Canberra), late morning light. Two
  colleagues stand near a bank of monitors showing simple network dashboards;
  a third leans casually in a doorway with a coffee cup, mid-conversation.
  Muted navy, cream, and warm amber palette. No text or logos in the image.

------------------------------------------------------------

==============================================================================
FROM: cheatsheet-episode-05-who-wins.html
------------------------------------------------------------------------------

Save as: images/s1-slamap-scope.png

Prompt (paste into image generator):
  A clean technical diagram contrasting two views side by side: on the left, a
  single magnifying glass over one FortiGate SD-WAN member icon labelled
  "sla_map — one session, one member, frozen at birth"; on the right, a live
  dashboard-style panel showing multiple member icons simultaneously labelled
  "diagnose sys sdwan health-check — all members, live, always." Muted navy
  and warm cream palette, minimal, no text other than the two labels, flat
  vector style, no logos.

------------------------------------------------------------
