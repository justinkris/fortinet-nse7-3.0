<Session 57 Extra — HA Failover and BGP Convergence>
============================================================

Save as: ha1-topo.png

Title: One hub, two spokes — and two completely different failure profiles.

Prompt:
Flat-vector enterprise topology illustration, cream #faf5e9 background, bold soft-brown outlines, modern clean vector style, no gradients, no photorealism, generous white space, readable as a study diagram rather than a vendor stencil. LEFT SIDE: a single large muted-blue rounded rectangle labelled 'DC' containing TWO identical stacked firewall icons with a short sync arrow between them and a small label 'HA CLUSTER'. Inside the DC box, a small coral chip icon labelled 'NP7' with a tiny lock symbol beside it and the caption 'IPsec offloaded in hardware'. RIGHT SIDE, upper: a muted-blue rounded rectangle labelled 'SYDNEY' containing TWO stacked firewall icons with a sync arrow, marked 'ACTIVE-PASSIVE', and a small cloud-server icon with the caption 'VM — everything in software'. RIGHT SIDE, lower: a smaller rectangle labelled 'MELBOURNE' containing ONE single firewall icon with the caption 'standalone — no failover'. Connect DC to each site with a thick sage-green tunnel drawn as a long rounded pipe with a small padlock icon at its midpoint, labelled 'IPsec'. Inside each pipe, draw a thin muted-blue line labelled 'BGP' to show the peering riding inside the tunnel. Beside the DC-Sydney pipe, place a small amber warning tag reading 'expected failure: FAILOVER'. Beside the DC-Melbourne pipe, a sage tag reading 'expected failure: GONE'. In the lower left corner, a small legend box in muted gold with two lines: 'FGCP syncs IPsec SAs — the tunnel survives' and 'FGCP does not sync the BGP daemon — the peering does not'. Do NOT include human characters, vendor logos, IP addresses, or dense interface labels. Purpose: anchor that the same hub can require opposite convergence tools on two different neighbours.

============================================================
